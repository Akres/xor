{
  "name": "@xor/xor-client-schema",
  "version": "2.0.0",
  "description": "Schema for api client, i.e. what the client must follow to be plugged to the API server.",
  "main": "./lib/index.js",
  "author": "Jiri Kantor",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf ./lib",
    "lint": "eslint . --ext .ts,.js --ignore-path ../../../.eslintignore"
  },
  "devDependencies": {
    "@xor/eslint-config": "^1.1.0",
    "@xor/typescript-config": "^1.0.0",
    "@xor/xor-api-schema": "^1.0.0"
  }
}
