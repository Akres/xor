{
  "name": "@xor/xor-client-schema",
  "version": "1.0.0",
  "description": "Schema of communication between api server and clients",
  "main": "./lib/index.js",
  "author": "Jiri Kantor",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf ./lib",
    "lint": "eslint . --ext .ts,.js --ignore-path ../../../.eslintignore"
  },
  "devDependencies": {
    "@xor/eslint-config": "^1.1.0",
    "@xor/typescript-config": "^1.0.0"
  }
}
