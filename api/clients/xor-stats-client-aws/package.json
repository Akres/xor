{
  "name": "@xor/xor-stats-client-aws",
  "version": "1.0.0-pre",
  "description": "Stats client that stores the stats in an AWS DynamoDB",
  "main": "./lib/index.js",
  "author": "Jiri Kantor",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf ./lib",
    "lint": "eslint . --ext .ts,.js --ignore-path ../../../.eslintignore"
  },
  "devDependencies": {
    "@xor/eslint-config": "^1.1.0",
    "@xor/typescript-config": "^1.0.0",
    "@xor/xor-stats-client-domain": "^1.0.0"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.41.0",
    "@xor/xor-config": "^1.1.0"
  }
}
